!!! 5
%html
  %head
    %title store_name
    = stylesheet_link_tag 'application', media: 'all'
    = javascript_include_tag 'application'
    = csrf_meta_tags
  %body
    .container-fluid
      .navbar.navbar-default
        = render 'layouts/header'
      - flash.each do |name, msg|
        = content_tag :div, msg, class: "#{name}-warning"
      = yield